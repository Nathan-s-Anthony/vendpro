"use client";

export default function Bars({ startValue, endValue }: { startValue: string, endValue: string }) {
    return (
        <div className="bg-blue-500">
            test
        </div>
    )

}